# Paraglide JS é gerado (i18n:compile) — não formatar nem versionar formatação.
# O hook de prettier roda da raiz do monorepo, então precisa do ignore aqui
# (os .prettierignore por-subprojeto não se aplicam à invocação da raiz).
**/paraglide/
**/src/paraglide/

# Outros artefatos gerados / vendorizados
**/dist/
**/build/
**/.vectora/
graphify-out/

# Conteúdo Hugo (docs/) usa shortcodes `{{< ... >}}` que o parser markdown
# do Prettier não entende — ele corrompe o fechamento multi-linha (vira
# "> }}" com espaço, quebra `hugo build`). O módulo vendorizado (docs/_vendor/)
# já é excluído globalmente no .pre-commit-config.yaml, mas o prettier roda
# fora do pre-commit também (editor, scons lint futuro) — ignore aqui também.
docs/content/
docs/_vendor/
