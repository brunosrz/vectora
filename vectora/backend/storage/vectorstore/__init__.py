"""Vector store nativo — `VectorStoreBackend` (Protocol) + implementações
`LanceDBBackend` (lite) e `QdrantBackend` (complete). Ponto de entrada:
`backend.storage.factory.get_vector_store_backend()`.
"""

from __future__ import annotations
