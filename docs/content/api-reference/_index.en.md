---
title: API Reference
weight: 8
sidebar:
  open: true
---

Running your own self-hosted instance and want to automate against the same API the frontend uses (chat, RAG, gateways, settings)? See [Self-Hosted Integration](./self-hosted-integration) — it's the real, documented internal API, without a stable public contract (endpoint shapes can change between releases).
