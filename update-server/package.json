{
  "name": "vectora-update-server",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Workers manifest server for Vectora desktop auto-updates.",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "release": "tsx scripts/release.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0",
    "tsx": "^4.19.0"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "yaml": "^2.6.0"
  }
}
