# Conexões lentas: dá mais tempo e mais tentativas para baixar pacotes grandes
# (ex.: o binário do supabase CLI, ~77 MB, estoura o timeout padrão de 60s).
fetch-timeout=600000
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
